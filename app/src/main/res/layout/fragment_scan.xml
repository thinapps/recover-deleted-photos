<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="@dimen/space_24"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- scan ui content (toggled visible) -->
    <LinearLayout
        android:id="@+id/scanContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <TextView
            android:id="@+id/totalLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/total_files_label"
            android:textAppearance="@style/TextAppearance.Material3.TitleMedium" />

        <FrameLayout
            android:id="@+id/pulseContainer"
            android:layout_width="@dimen/scan_pulse_size"
            android:layout_height="@dimen/scan_pulse_size"
            android:layout_marginTop="@dimen/space_12">

            <View
                android:id="@+id/pulse1"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:alpha="0.0"
                android:background="@drawable/pulse_circle"
                android:outlineProvider="background"
                android:clipToOutline="true" />

            <View
                android:id="@+id/pulse2"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:alpha="0.0"
                android:background="@drawable/pulse_circle"
                android:outlineProvider="background"
                android:clipToOutline="true" />

            <TextView
                android:id="@+id/totalCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="0"
                android:textStyle="bold"
                android:textSize="@dimen/scan_count_text_size"
                android:fontFamily="monospace"
                android:textColor="@color/md_blue_700" />
        </FrameLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/cancelButton"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_marginTop="@dimen/space_24"
            android:text="@string/cancel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
    </LinearLayout>

    <!-- permission/empty/error state container (toggled visible) -->
    <LinearLayout
        android:id="@+id/stateContainer"
        android:layout_width="match_parent"
        android:layout_height="@dimen/layout_zero"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/space_24"
        android:visibility="gone">

        <TextView
            android:id="@+id/stateTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Material3.TitleLarge"/>

        <TextView
            android:id="@+id/stateMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/space_8"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/statePrimary"
            style="@style/Widget.Material3.Button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_16"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/stateSecondary"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_8"
            android:visibility="gone"/>
    </LinearLayout>

</LinearLayout>
