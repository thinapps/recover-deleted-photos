<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- app theme using material 3 daynight without action bar -->
    <style name="Theme.RecoverDeletedPhotos" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/md_seed</item>
        <item name="colorSecondary">@color/md_secondary</item>
        <item name="colorSurface">@color/md_surface</item>
        <item name="colorOnSurface">@color/md_on_surface</item>
        <item name="colorOutline">@color/md_outline</item>
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.App.MaterialAlertDialog</item>

        <!-- apply our toolbar style globally -->
        <item name="toolbarStyle">@style/Widget.App.Toolbar.Tinted</item>

    </style>

    <!-- material toolbar styled to use surface/onSurface so contrast works in light and dark -->
    <style name="Widget.App.Toolbar.Tinted" parent="Widget.Material3.Toolbar">
        <!-- background and text/icon colors -->
        <item name="android:background">?attr/colorSurface</item>
        <item name="titleTextColor">?attr/colorOnSurface</item>
        <item name="subtitleTextColor">?attr/colorOnSurface</item>
        <item name="navigationIconTint">?attr/colorOnSurface</item>

        <!-- themed context for menu item icon tint and ripple -->
        <item name="android:theme">@style/ThemeOverlay.App.Toolbar.Surface</item>

        <!-- overflow popup uses matching colors -->
        <item name="popupTheme">@style/ThemeOverlay.App.Toolbar.Popup</item>
    </style>

    <!-- overlay applied to the toolbar view itself -->
    <style name="ThemeOverlay.App.Toolbar.Surface" parent="ThemeOverlay.Material3.ActionBar">
        <item name="colorControlNormal">?attr/colorOnSurface</item>
        <item name="android:textColorPrimary">?attr/colorOnSurface</item>
    </style>

    <!-- overlay for the overflow popup menu -->
    <style name="ThemeOverlay.App.Toolbar.Popup" parent="ThemeOverlay.Material3">
        <item name="colorControlNormal">?attr/colorOnSurface</item>
        <item name="android:textColor">?attr/colorOnSurface</item>
    </style>

    <!-- privacy policy dialog with explicit text sizes -->
    <style name="ThemeOverlay.App.MaterialAlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="materialAlertDialogTitleTextStyle">@style/TextAppearance.App.Dialog.Title</item>
        <item name="materialAlertDialogBodyTextStyle">@style/TextAppearance.App.Dialog.Body</item>
        <item name="buttonBarPositiveButtonStyle">@style/Widget.App.Button.Dialog</item>
    </style>

    <style name="TextAppearance.App.Dialog.Title" parent="MaterialAlertDialog.Material3.Title.Text">
        <item name="android:textSize">@dimen/dialog_title_text_size</item>
    </style>

    <style name="TextAppearance.App.Dialog.Body" parent="MaterialAlertDialog.Material3.Body.Text">
        <item name="android:textSize">@dimen/dialog_body_text_size</item>
    </style>

    <style name="Widget.App.Button.Dialog" parent="Widget.Material3.Button.TextButton.Dialog">
        <item name="android:textSize">@dimen/dialog_button_text_size</item>
    </style>

    <!-- overlay for radio buttons: ring uses onSurface, center uses white surface, dot uses primary -->
    <style name="ThemeOverlay.App.RadioButton.WhiteCenter" parent="ThemeOverlay.Material3">
        <item name="colorControlNormal">?attr/colorOnSurface</item>
        <item name="colorControlActivated">?attr/colorPrimary</item>
        <item name="colorSurface">@android:color/white</item>
    </style>

    <!-- scoped white checkbox overlay for the grid cell FrameLayout -->
    <style name="ThemeOverlay.App.Checkbox.Light" parent="ThemeOverlay.Material3">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
        <item name="colorOnSurface">@android:color/white</item>
    </style>

</resources>
